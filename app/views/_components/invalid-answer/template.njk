{# 
{% if data.temp.invalidString and data.settings.highlightInvalidAnswers | falsify %}
  {% set userValue = data.temp.invalidString %}
  {% set insetContent %}

    <h2 class="govuk-heading-m app-inset-text--invalid-answer__heading">Answer not recognised</h2>

    <p class="govuk-body">The applicant’s answer was <span class="govuk-!-font-weight-bold">{{userValue}}</span>.</p>

    <p class="govuk-body">Choose the closest available match.</p>

    {{params.html | safe}}
    
  {% endset %}
  
  {{ govukInsetText({
    html: insetContent,
    classes: 'app-inset-text--invalid-answer app-inset-text--narrow govuk-!-padding-top-0 govuk-!-padding-bottom-0'
  }) }}

{% else %} #}
  {{params.html | safe}}
{# {% endif %} #}



